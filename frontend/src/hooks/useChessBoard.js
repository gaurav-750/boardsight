/**
 * Custom hook encapsulating all chess-board state and logic.
 *
 * Will own the chess.js Game instance and expose:
 *  - current FEN
 *  - move handler (validates via chess.js, updates state)
 *  - undo / reset helpers
 *  - board orientation
 */
export default function useChessBoard() {
  // TODO: initialise chess.js game, wire up state
  return {};
}
